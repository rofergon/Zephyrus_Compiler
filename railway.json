{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd api-server && rm -f package-lock.json && npm install --production"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "cd api-server && npm start"
  }
} 